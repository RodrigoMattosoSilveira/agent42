#!/bin/bash

# Usage
# feature version

feature() {
  regex='^[[:digit:]]+(\.[[:digit:]]+)+(-[[:alnum:]]+)+'
  if [[ $1 =~ $regex ]]; then
    echo "✅ Version $1 is a feature branch version"
    return 0
  else
    echo "🚫 Version $1 is not a feature branch version"
    return 1
  fi
}
